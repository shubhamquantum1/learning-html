<!-- Level 2 — Topic 1: Reusable Code with Functions-->
<!-- ------------------------------------------------------------------------ -->
<!-- 
        Functions  are block of code that are used to perform a particular task and repeated task

         function functionname(par1, par2, ...) {
            //code to be executed
            return ...;
                    
       // x. Practice[1]:
        // a. Write a function to calculate the product of two numbers
        // b. Write a function to convert celcius to fahrenheit
        // c. Write a function to convert fahrenheit to celcius
                    
        
        Your Task:
        Modify previous code to allow display of any students (using functions)

        1. Open your script.js file and clear the existing code.
        2. Define function at the top that accepts studentName, weeklyGoalHours, daysPlanned, daysCompleted, totalHoursStudied as attributes
        3. Print the final results using a Template Literal (with backticks ` and ${}) so it outputs this exact message format in the browser console:
                Dashboard for [studentName]:
                You have studied an average of X hours per day.
                You need to study Y more hours to reach your goal of [weeklyGoalHours] hours.
                (X and Y must be the calculated variables!)
        
        4. Save the file and refresh your browser.
-->