<!-- Phase 4 — Topic 3: Optimization & Deployment 3: Optimization complete. Pushed to Github-->
<!-- --------------------------------------------------------------------------------- -->
<!-- 
    Your Task:
    Part-1: Web servers (including GitHub Pages) always look for a file named index.html to act as the default homepage of a folder. If your homepage is named portfolio.html, users would have to type the exact filename in the URL to visit your site. If we rename it to index.html, the browser loads it automatically!
    1. Rename the Homepage: - In optimization-and-deployment/project 1 folder, rename portfolio.html to index.html.
    2. Update Navigation Links in all HTML page from <a href="portfolio.html" ...> Home </a> to <a href="index.html" ...> Home </a>.
    3. Push to GitHub


    Part-2: Activating GitHub Pages => GitHub has a built-in feature that hosts standard static websites (HTML, CSS, JS) directly from a repository for free.
    1. Open your web browser, go to github.com, and open your learning-html repository page.
    2. Click on the Settings tab at the top of the repository page (it has a gear icon).
    3. In the left-hand sidebar, look for the "Code and automation" section and click on Pages.
    4. Under the Build and deployment settings:
        * Source: Make sure this is set to "Deploy from a branch" (this is the default).
        * Branch:
            - Click the dropdown that says "None" and change it to main.
            - Keep the adjacent folder dropdown set to / (root).
            - Click the Save button.
    5. Wait about 1 to 2 minutes. GitHub is building your live website in the background! 
    6. Refresh the page. A banner will appear at the very top of the Page settings screen saying:
    Your site is live at https://YOUR_USERNAME.github.io/learning-html/
    Note-1 => Because your final code is inside a subfolder, your homepage URL will be: https://YOUR_USERNAME.github.io/learning-html/optimization-and-deployment/project%201/
    Note-2 => (Notice how you don't even need to type /index.html at the end! Because you renamed the file to index.html, the browser automatically finds and loads it).
    Note-3 => Make sure your repository is public otherwise you cannot move beyond step-3 meaning it'll ask you to upgrade(subscribe) github account at step-4. To avoid that(for testing), change github repo from private to public.



    NOTE's to yourself.
    ===================
    Shubham, this is a major milestone! Take a second to look at what you just achieved.
    In a matter of weeks, you went from knowing absolutely nothing about code, to:

    1. Phase 1: HTML Structure (12 checkpoints completed, from basic boilerplate to complex inputs and tables) => Writing semantic, clean HTML structures by hand.
    2. Phase 2: CSS Styles & Layouts (Mastered typography, HSL colors, Box Model, Flexbox alignment, hover transitions, and mobile media queries) => Creating a modern dark-theme CSS design system with fluid layout alignment (Flexbox), hover animations, and complete mobile responsiveness (Media Queries).
    3. Phase 3: JavaScript Interactivity (Wrote reusable logic, DOM selection/modification scripts, persistent theme controls using localStorage, and dynamic form validation) => Writing JavaScript logic to capture form submissions, validate inputs, toggle themes dynamically, and persist settings in the browser console using localStorage.
    4. Phase 4: Optimization & Deployment (Added SEO metadata, accessibility lang targets, cleaned files, pushed code to GitHub, and hosted it via GitHub Pages). => Optimizing the site for search engines and accessibility, pushing the code to Git, and hosting it live.

    Your live, interactive portfolio is officially running online at: 👉 shubhamquantum1.github.io/learning-html/optimization-and-deployment/project%201/
-->